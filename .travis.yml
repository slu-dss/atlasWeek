language: r
sudo: false
cache: false

r:
  - oldrel
  - release
  - devel

os:
  - linux
  - osx

matrix:
  fast_finish: true
  allow_failures:
    - r: devel
      os: osx

script:
  - Rscript -e "rmarkdown::render('notebook/complete/atlas-01-dataCleaning-complete.Rmd')"
  - Rscript -e "rmarkdown::render('notebook/complete/atlas-02-dataExploration-complete.Rmd')"
  - Rscript -e "rmarkdown::render('notebook/complete/atlas-02-dataPlotting-complete.Rmd')"
